{
  "name": "pc-device-info",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:node": "node-gyp clean && node-gyp configure && node-gyp build",
    "build:electron": "node-gyp clean && electron-rebuild .",
    "test": "npm run build:node && node test.js"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:zp1996/pc-device-info.git"
  },
  "license": "ISC",
  "dependencies": {
    "bindings": "^1.3.1",
    "nan": "^2.11.1"
  },
  "devDependencies": {
    "electron": "3.0.10",
    "electron-rebuild": "^1.8.2"
  }
}
